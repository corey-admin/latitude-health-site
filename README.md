# Latitude Health Innovations

Public static website for **latitude-health.org**, served via GitHub Pages.

Contains only the published static site (`index.html`, `styles.css`, `articles.json`).
The research-search automation and review queue live in a separate private repository.
